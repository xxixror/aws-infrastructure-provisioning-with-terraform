# ============================================================
# Load Balancer 모듈 출력값 정의
# 생성된 로드밸런서 정보를 다른 모듈에서 참조할 수 있도록 내보냄
# ============================================================

# 로드밸런서 DNS 이름 출력
# 브라우저에서 이 DNS로 접속하면 로드밸런서로 연결됨
# Route53에서 CNAME 레코드로 등록하여 사용자 정의 도메인과 연결 가능
output "lb_dns_name" {
  description = "로드밸런서의 DNS 이름 (예: my-lb-1234.ap-northeast-2.elb.amazonaws.com)"
  value       = aws_lb.this.dns_name
}
